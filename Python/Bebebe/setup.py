from setuptools import setup, find_packages


setup(
    name = "Lab-2",
    version = "0.1",
    description = "Lab #2, all the tasks",
    packages = find_packages(),
    python_requires = ">=3.6"
)
